# Example shape for a local runtime secret file.
# Copy this structure to ${user.home}/.mutuus/sample-api/local.yml and put real
# values there. Do not commit real secret files.
spring:
  datasource:
    password: "<local database password>"
  data:
    redis:
      password: "<local redis password>"
