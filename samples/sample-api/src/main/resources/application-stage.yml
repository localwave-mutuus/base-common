# stage 프로파일: 실제 인프라 접속값은 환경변수/시크릿으로 주입한다(파일에 비밀 미기재).
# 활성화:  --spring.profiles.active=stage
demo:
  environment: stage

# 예시) 스테이지 DB/Redis 는 배포 환경의 환경변수로 주입:
# spring:
#   datasource:
#     url: ${DB_URL}
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#   data:
#     redis:
#       url: ${REDIS_URL}
